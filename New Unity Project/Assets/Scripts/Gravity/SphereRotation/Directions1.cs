using UnityEngine;
using System.Collections;

public class Directions1 : MonoBehaviour {
	

	/*public static Vector3 xDirection;
	public static Vector3 zDirection;
	Vector3 point = Vector3.zero;
	public float angle = 1f;

	void Start () {
		xDirection = new Vector3(transform.position.x, 0, 0);
		zDirection = new Vector3(0,transform.position.z, 0);
	}

	public void Rotate(Vector3 axis, string direction) {
		if (direction == "z") {
			transform.RotateAround (point, axis, angle);
			zDirection = new Vector3(
		} 
		else if(direction == "x") {
			transform.RotateAround (point, axis, angle);
		}

	}*/
}